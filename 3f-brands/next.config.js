/** @type {import('next').NextConfig} */
const nextConfig = {
    fastRefresh: true,
    // Add this if you're using app directory
    experimental: {
      appDir: true
    }
  };
  
  module.exports = nextConfig;